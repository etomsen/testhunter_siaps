package it.unibz.testhunter.plugin.hudson.siaps;

import java.net.URL;
import java.sql.Timestamp;

import it.unibz.testhunter.shared.IBuild;

public interface IBuildFactory {
	
	public IBuild create(URL url, Long number, Timestamp start, String result);
}
